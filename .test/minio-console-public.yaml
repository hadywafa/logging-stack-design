apiVersion: v1
kind: Service
metadata:
  name: minio-console-public
  namespace: logging
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
    - name: console
      port: 9001
      targetPort: 9001
  selector:
    app: minio
    release: minio
