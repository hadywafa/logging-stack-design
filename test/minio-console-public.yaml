apiVersion: v1
kind: Service
metadata:
  name: minio-console-public
  namespace: logging
spec:
  type: LoadBalancer
  ports:
    - port: 9001
      targetPort: 9001
  selector:
    app.kubernetes.io/name: minio
